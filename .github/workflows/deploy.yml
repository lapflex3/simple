name: Deploy to FTP Server

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.4
        with:
          server: ${{ ftp.nasadef.com.my }}
          username: ${{ razi@nasadef.com.my }}
          password: ${{ Nikrazif@1 }}
          local-dir: ./
          server-dir: /
          protocol: ftps
          state-name: ftp_deploy.json
          dangerous-allow-ftp-without-tls: true
